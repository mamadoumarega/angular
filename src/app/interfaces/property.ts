// tslint:disable-next-line:no-empty-interface
export interface Property {
  title: string;
  category: string;
  surface: string;
  rooms: string;
  description?: string;
  price: string;
  sold: boolean;
  photos?: any[];
}
